float t= 0;

void setup() {
  size(600,400);
}

void draw() {
  background(220);
  fill(#E51010);
  noStroke();
  
  t = t+0.05;
  
  float x = noise(t);
  x = map(x,0,1,0,width);
  rect(x,height/2,90,20);
}
